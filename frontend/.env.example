# Trading Dashboard Frontend Configuration

# =============================================================================
# Development Proxy Settings (Vite Dev Server)
# =============================================================================
# These control where the Vite dev server proxies API/WebSocket requests to
# Only used during `npm run dev`, not in production builds
VITE_DEV_BACKEND_URL=http://127.0.0.1:8000
VITE_DEV_BACKEND_WS_URL=ws://127.0.0.1:8000

# =============================================================================
# Production Runtime Settings (Optional)
# =============================================================================
# Only needed for custom deployments where frontend and backend are separate
# For standard same-origin deployments, leave these unset (relative URLs work)

# API base URL (default: /api/v1)
# VITE_API_BASE_URL=/api/v1

# WebSocket base path (default: /ws)
# VITE_WS_BASE_PATH=/ws

# =============================================================================
# App Customization
# =============================================================================
# Browser tab title (default: Trading Dashboard)
# VITE_APP_TITLE=Trading Dashboard
